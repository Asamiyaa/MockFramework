1.对比zookeeper 实现

2.从状态模式到复杂业务模型下的有限状态机 --> if/else 在复杂，存在中心(票)，不同场景(贴现/质押/..) 业务嵌套 --登记中心
  每个业务流程图肯定是明确的。在脑子里面，写代码因为状态机的存在将大逻辑拆解成类 Method Status xxhist

3.没有想象的那么智能，每一个自动都是‘ 状态变化 ’

4.追加历史记录来记录完整的流转过程